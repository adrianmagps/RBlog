<li id="<%= category.name %>_<%= category.id %>" class="list-group-item" style="margin-right: 5px;">
  <%= category.name %>
  <% if can? :destroy, Category %>
      <%= link_to '', category,:class => 'fa fa-remove', :method => :delete, confirm: 'Are you sure?', :remote => true %>
  <% end %>
</li>